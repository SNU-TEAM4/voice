# 기계학습 및 딥러닝 기반 완전 AI 생성 음악 탐지

## 미지 생성기 일반화와 오디오 압축 강건성 분석

## 1. 연구 개요

### 한 줄 요약

원곡 계열 간 데이터 누수를 차단한 REAL–TTA 음악 데이터에서 수작업 음향
특징 기반 SVM, Log-Mel CNN, 음악 사전학습 모델 MERT를 비교하고, 학습에서
보지 못한 음악 생성기와 MP3 압축 환경에서도 성능이 유지되는지 평가한다.

### 서비스 관점의 질문

> AI 생성 음악 탐지기가 학습에서 본 생성기만 잘 찾는 것은 아닐까?
> 업로드 과정에서 음원이 압축되어도 판별 결과를 신뢰할 수 있을까?

본 연구의 결과물은 저작권 침해나 인간의 창작 여부를 확정하는 도구가
아니라, 플랫폼에서 추가 검토가 필요한 음원을 선별하는 1차 탐지 모듈로
정의한다.

## 2. 연구 대상과 범위

### 판별 대상

- REAL: 사람이 제작한 FMA 음악
- FAKE: 텍스트 설명만으로 전체 음원을 생성한 Echoes TTA 음악
- 제외: 실제 음원을 직접 조건으로 사용한 ATA, AI 커버, 일부 보컬만 합성한 음악

TTA는 FMA 원곡의 제목·장르에서 만든 스타일 설명을 참고하지만, 생성기에
원곡 파형을 직접 입력하지 않는다. 따라서 본 연구에서는 TTA를 “완전 AI
생성 음악”으로 조작적으로 정의한다.

### 연구 결과의 적용 범위

- 포함 장르: Electronic, Pop, Rock
- 포함 생성기: 12개
- 입력 단위: 10초 음악 구간
- 예측 단위: 10초 구간 및 전체 트랙
- 일반화 대상: 학습에서 제외한 음악 생성기, MP3 압축 조건

본 연구 결과를 모든 장르, AI 보컬만 합성한 음악, AI 편곡·리믹스까지
일반화해서 해석하지 않는다.

## 3. 연구 질문과 가설

### RQ1. ML과 DL 중 어떤 표현 방식이 탐지에 효과적인가?

> 수작업 음향 특징 기반 SVM보다 Log-Mel CNN 또는 음악 사전학습 표현이
> 완전 AI 생성 음악 탐지에 효과적인가?

- H1-1: Log-Mel CNN은 SVM보다 낮은 EER과 높은 ROC-AUC를 보일 것이다.
- H1-2: MERT 표현은 음악 구조를 학습했지만 생성 흔적 탐지에 특화된 것은
  아니므로, CNN보다 반드시 우수할 것이라고 가정하지 않고 실험으로 확인한다.

### RQ2. 처음 보는 음악 생성기도 탐지할 수 있는가?

> 특정 생성기의 FAKE를 학습에서 완전히 제외했을 때 성능은 얼마나 감소하는가?

- H2: 미지 생성기 조건에서는 모든 모델의 EER이 증가할 것이다.
- 비교 대상은 생성 특성이 다른 MusicGen과 Suno로 사전 지정한다.
  MusicGen은 공개 연구 모델·고정 길이 생성 사례, Suno는 상용 장문 음악
  생성 사례를 대표하며 두 생성기 모두 표본 수가 충분하다.

### RQ3. 압축된 음악에서도 탐지 성능이 유지되는가?

> 원본 테스트 음원을 MP3로 다시 압축했을 때 모델 성능은 얼마나 변하는가?

- H3-1: 낮은 비트레이트일수록 생성 흔적이 손실되어 성능이 하락할 것이다.
- H3-2: 학습 시 REAL과 FAKE에 동일 확률로 코덱 증강을 적용하면 압축
  조건의 성능 하락폭이 감소할 것이다.

### 보조 질문. 여러 구간을 결합하면 더 정확한가?

> 한 곡의 10초 구간 하나만 사용할 때보다 최대 3개 구간의 확률을 평균하면
> 트랙 단위 성능이 향상되는가?

## 4. 데이터 구성

### 확보된 데이터

| 클래스 | 출처 | 트랙 수 | 비고 |
|---|---|---:|---|
| REAL | FMA | 296 | 약 30초, 제목·아티스트로 Echoes와 연결 |
| FAKE | Echoes TTA | 3,162 | 완전 AI 생성, 12개 생성기 |
| 제외 | Echoes TTA | 3행 | 서로 다른 원곡이 동일 MusicGen 파일 경로를 사용 |
| 제외 | Echoes ATA | 1,303 | 원곡 파형을 조건으로 사용하므로 연구 범위 밖 |
| 합계 | REAL + TTA | 3,458 | 전 파일 디코딩 및 10초 이상 확인 완료 |

Echoes 공개 manifest의 TTA는 3,165행이지만, 서로 다른 원곡 3개가 동일한
`_musicgen_TTA_001.wav`를 가리키는 충돌이 확인되었다. 어느 행이 실제
파일과 대응하는지 판별할 수 없어 해당 파일과 세 행을 모두 제외했다.

Echoes manifest에는 FMA track ID가 없으므로 `원곡 제목 - 아티스트`로
FMA와 연결했다. 296개 이름은 모두 발견되었으나 17개는 동명 후보가 있어
장르 일치, 라이선스, 낮은 track ID 순의 고정 규칙으로 선택했다. 이 과정은
재현 가능하도록 별도 CSV에 기록한다.

### 현재 고정된 그룹 분할

| Split | 원곡 그룹 | REAL | FAKE |
|---|---:|---:|---:|
| Train | 207 | 207 | 2,237 |
| Validation | 45 | 45 | 482 |
| Test | 44 | 44 | 443 |

- 비율: 원곡 그룹 기준 약 70% / 15% / 15%
- 난수 seed: 42
- 장르 층화: Electronic, Pop, Rock 비율을 split마다 유지
- 그룹 키: `original_audio`
- 같은 FMA 원곡과 연결된 모든 TTA를 반드시 같은 split에 배치
- 분할 후 경로 누락 및 그룹 누수: 0건
- train, validation, test 모두 12개 생성기를 포함

무작위로 개별 파일을 나누면 같은 원곡에서 파생된 유사한 TTA가 학습과
테스트에 동시에 들어갈 수 있다. 본 연구에서는 원곡 그룹을 먼저 나눈 뒤
10초 구간을 생성하여 이 누수를 차단한다.

## 5. 탐색적 데이터 분석

다음 EDA를 모델 학습 전에 수행한다.

1. REAL/FAKE 트랙 수와 총 재생시간
2. 생성기 12종별 트랙 수와 길이 분포
3. 장르별 REAL/FAKE 분포
4. 파일 형식, 코덱, 샘플링레이트, 채널 분포
5. 동일 원곡 그룹의 REAL–TTA Log-Mel Spectrogram 비교
6. MFCC·스펙트럴 특징의 PCA 또는 UMAP 시각화
7. 색상을 label과 generator로 각각 표시하여 모델이 REAL/FAKE보다
   생성기 정체성을 쉽게 학습할 가능성 확인
8. 중복 경로, 손상 음원, FMA 중복 후보 등 품질 점검 결과 정리

샘플링레이트는 현재 16, 22.05, 32, 44.1, 48kHz가 섞여 있다. 이 분포를
그대로 모델에 노출하면 생성기나 클래스의 출처를 맞히는 지름길이 될 수
있으므로 공통 전처리가 필수다.

## 6. 전처리와 세그먼트 구성

### 공통 전처리

```text
원본 MP3/WAV 로드
→ mono 변환
→ 24,000 Hz 리샘플링
→ float waveform 변환
→ 10초 구간 추출
```

- 24kHz를 선택하는 이유: 모든 모델에 같은 파형을 제공하면서 음악 전용
  MERT의 공식 입력 샘플링레이트와 맞추기 위해서다.
- 무음 제거는 하지 않는다. 시작·종료부의 생성 특성도 탐지 정보일 수 있다.
- 클래스별로 다른 전처리를 적용하지 않는다.
- 원본 파일은 유지하고, 잘라낸 WAV를 대량 저장하지 않는다.
- segment manifest에 `sample_id`, `start_sec`, `duration`, `split`을 기록하고
  학습 시 해당 구간만 불러온다.

### 트랙당 구간 선택

- 트랙당 최대 3개의 10초 구간을 사용한다.
- 30초 이상이면 시작·중간·끝에서 서로 겹치지 않도록 균등하게 선택한다.
- 20~30초이면 2개, 10~20초이면 1개를 사용한다.
- 30초에 근접한 FMA 음원은 부동소수점 오차를 고려해 3개로 처리한다.

현재 파일 길이를 적용하면 예상 세그먼트는 다음과 같다.

| Split | REAL 구간 | FAKE 구간 |
|---|---:|---:|
| Train | 621 | 6,299 |
| Validation | 135 | 1,355 |
| Test | 132 | 1,242 |
| 합계 | 888 | 8,896 |

총 세그먼트는 약 9,784개다. 트랙당 최대 3개로 제한하여 긴 AI 곡이 학습을
지배하는 문제를 방지한다.

### 학습 불균형 처리

훈련 배치는 다음 계층으로 균형 추출한다.

1. 배치의 REAL/FAKE 비율을 1:1로 구성
2. FAKE 절반은 12개 생성기에서 가능한 한 균등하게 추출
3. 같은 트랙의 세 구간이 한 배치에 반복되지 않도록 추출

SVM에는 같은 원리를 반영한 `sample_weight`를 적용하고, CNN에는
balanced sampler를 사용한다. Validation과 Test는 재표본화하지 않는다.

## 7. 비교 모델

### Model 0. 단순 기준선

- Logistic Regression
- 입력: Model 1과 같은 수작업 특징
- 목적: 비선형 SVM이 실제로 필요한지 확인

### Model 1. 수작업 특징 + SVM

```text
10초 파형
→ MFCC 40개 + Δ + Δ²
→ Spectral centroid/bandwidth/rolloff/flatness/contrast
→ RMS, Zero-crossing rate
→ 각 특징의 시간축 mean/std
→ StandardScaler
→ RBF-SVM
```

- 조정값: `C`, `gamma`, `class/sample weight`
- 모든 특징 스케일러는 Train에서만 적합한다.
- 특징 중요도 해석이 필요하면 동일 특징의 Logistic Regression 계수를
  보조적으로 사용한다.

### Model 2. Log-Mel Spectrogram + 경량 CNN

```text
10초 파형
→ 128-bin Log-Mel Spectrogram
→ Conv-BN-Activation-Pooling 블록 4개
→ Global Average Pooling
→ Dropout
→ Sigmoid output
```

- 예시 설정: `n_fft=1024`, `hop_length=240`, `n_mels=128`, `fmax=12000`
- 파라미터 수는 약 2M 이하로 제한
- Loss: Binary Cross Entropy
- Optimizer: AdamW
- Early stopping 기준: Validation EER
- 과적합 확인: train/validation loss, EER 곡선

### Model 3. Frozen MERT-v1-95M + 분류기

```text
24kHz 10초 파형
→ 사전학습 MERT-v1-95M(동결)
→ 시간축 평균 pooling
→ Logistic Regression 또는 2-layer MLP
→ REAL/FAKE
```

MERT는 음악 이해를 위해 사전학습된 95M 규모 모델이다. 전체를 미세조정하지
않고 임베딩을 한 번 추출해 저장하면 모델 비교와 미지 생성기 실험을
현실적인 계산량으로 수행할 수 있다.

### 핵심 비교 의미

| 모델 | 표현 학습 방식 | 연구상 의미 |
|---|---|---|
| SVM | 사람이 음향 특징 설계 | 전통 ML 기준선 |
| CNN | 데이터에서 시간–주파수 패턴 학습 | 처음부터 학습한 DL |
| MERT | 대규모 음악에서 사전학습 | 음악 전이학습 효과 |

## 8. 실험 설계

### Experiment 1. 표준 In-domain 모델 비교

모든 생성기가 Train에 포함된 표준 분할에서 세 모델을 비교한다.

```text
Train → 모델 학습
Validation → 하이퍼파라미터·threshold 결정
Test → 한 번만 최종 평가
```

테스트 결과를 본 뒤 하이퍼파라미터나 threshold를 바꾸지 않는다.

### Experiment 2. 미지 생성기 일반화

MusicGen과 Suno를 각각 한 번씩 완전히 제외한다.

```text
예: MusicGen holdout

Train: REAL + 나머지 11개 생성기 FAKE
Validation: REAL + 나머지 11개 생성기 FAKE
Test: REAL + MusicGen FAKE만 사용
```

- 표준 모델이 MusicGen 테스트 부분집합에서 낸 성능을 `seen`으로 기록
- MusicGen을 제외하고 다시 학습한 모델 성능을 `unseen`으로 기록
- `unseen EER - seen EER`을 일반화 격차로 계산
- Suno에도 동일 절차 적용
- 시간이 허용되면 frozen MERT와 SVM에 한해 12개 전체 LOPO로 확장

### Experiment 3. MP3 압축 강건성

최종 모델과 threshold를 고정한 뒤 Test의 REAL과 FAKE 모두에 동일하게
다음 변환을 적용한다.

| 조건 | 설명 |
|---|---|
| Original | 공통 디코딩·24kHz 리샘플링만 적용 |
| MP3-128k | 128kbps로 재압축 후 디코딩 |
| MP3-64k | 64kbps로 재압축 후 디코딩 |

추가로 CNN에 대해 다음 두 학습 조건을 비교한다.

- 기본 학습
- REAL/FAKE 모두에 같은 확률로 MP3 코덱 증강을 적용한 학습

압축 조건에서 EER 변화량과 REAL 오탐률 변화량을 보고한다.

### Experiment 4. 구간 예측과 트랙 예측

- Segment-level: 각 10초 구간을 개별 평가
- Track-level: 동일 `sample_id`의 최대 3개 확률을 평균
- 첫 구간 하나의 결과와 세 구간 평균 결과를 비교

길이가 긴 FAKE에만 더 많은 기회를 주지 않도록 트랙당 최대 3개라는 동일한
상한을 적용한다.

## 9. 평가 지표와 threshold

### 주 지표

- EER: 낮을수록 좋음
- ROC-AUC: 높을수록 좋음

### 보조 지표

- Macro-F1
- Balanced Accuracy
- PR-AUC
- REAL False Positive Rate: 사람 음악을 AI라고 판단한 비율
- FAKE Miss Rate: AI 음악을 사람 음악이라고 통과시킨 비율
- Confusion Matrix

Accuracy는 FAKE가 훨씬 많은 현재 데이터에서 과대평가될 수 있으므로 주
지표로 사용하지 않는다.

Threshold는 Validation에서 EER 기준으로 한 번 결정하고 Test, 미지 생성기,
압축 조건에서 고정한다. 필요하면 서비스 시나리오를 위해 “FAKE Recall 95%
이상에서 REAL 오탐률 최소” threshold를 별도로 제시하되 주 결과와 구분한다.

### 통계적 안정성

- CNN은 최소 3개 초기화 seed의 평균과 표준편차 보고
- Test 트랙을 그룹 단위로 1,000회 bootstrap하여 95% 신뢰구간 계산
- 세그먼트가 아니라 트랙 또는 원곡 그룹을 재표본화하여 의사 반복을 방지

## 10. 오류 분석

다음 기준으로 오분류를 분석한다.

1. 생성기별 EER 및 FAKE miss rate
2. 장르별 EER
3. REAL을 FAKE로 잘못 판단한 곡의 Log-Mel Spectrogram
4. FAKE를 REAL로 통과시킨 곡의 생성기와 음향 특징
5. Original 대비 MP3-64k에서만 오분류된 곡
6. 모델 간 공통 오분류와 모델별 고유 오분류
7. Grad-CAM 또는 주파수 마스킹으로 CNN이 참고한 시간–주파수 영역 확인

MERT 또는 PCA 공간이 label보다 generator별로 더 뚜렷하게 분리된다면,
모델이 일반적인 AI 흔적보다 특정 생성기의 서명을 학습했다는 근거로
해석한다.

## 11. 예상 결과표

### 표준 성능

| 모델 | Segment EER ↓ | Track EER ↓ | ROC-AUC ↑ | Macro-F1 ↑ | REAL FPR ↓ |
|---|---:|---:|---:|---:|---:|
| 특징 + SVM | | | | | |
| Log-Mel CNN | | | | | |
| MERT + Head | | | | | |

### 미지 생성기

| 모델 | 생성기 | Seen EER | Unseen EER | 일반화 격차 |
|---|---|---:|---:|---:|
| SVM/CNN/MERT | MusicGen | | | |
| SVM/CNN/MERT | Suno | | | |

### 압축 강건성

| 모델 | Original EER | MP3-128k EER | MP3-64k EER | 64k 변화량 |
|---|---:|---:|---:|---:|
| 특징 + SVM | | | | |
| Log-Mel CNN | | | | |
| MERT + Head | | | | |

## 12. 구현 우선순위

### 반드시 완료

1. 데이터 EDA 및 품질 이슈 보고
2. 누수 없는 그룹 분할과 10초 segment manifest
3. 특징 + SVM
4. Log-Mel CNN
5. Segment/Track 성능 비교
6. MusicGen·Suno 미지 생성기 실험
7. 생성기별 오류 분석

### 시간이 허용되면 완료

1. MERT-v1-95M frozen embedding
2. MP3 압축 강건성 및 코덱 증강
3. 12개 전체 LOPO
4. 오디오 업로드 데모

MERT가 환경 문제로 지연되어도 SVM–CNN 비교와 두 생성기 holdout만으로
핵심 연구 질문을 완성할 수 있도록 범위를 구성한다.

## 13. 위험요인과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| REAL 296 vs FAKE 3,162 불균형 | Accuracy 왜곡, REAL 과적합 | label·provider 균형 sampling, EER·Macro-F1 사용 |
| 샘플링레이트·파일 형식 차이 | 출처를 학습하는 shortcut | 모두 mono 24kHz로 디코딩, 압축 강건성 별도 평가 |
| 같은 원곡 계열 누수 | 테스트 성능 과대평가 | `original_audio` 그룹 분할 후 세그먼트 생성 |
| FMA track ID 미제공 | 원 논문의 300곡 완전 재현 어려움 | 296개 이름 전수 매칭, 선택 규칙·중복 후보 공개 |
| MusicGen 충돌 경로 | 잘못된 원곡 연결 | 충돌한 3행과 파일 전부 제외 |
| 긴 FAKE가 학습 지배 | 특정 생성기·길이 편향 | 트랙당 최대 3구간, provider 균형 sampling |
| 미지 생성기 성능 하락 | 실서비스 일반화 제한 | seen/unseen 격차를 핵심 결과로 보고 |

## 14. 프로젝트 산출물

- 데이터 품질 및 EDA 노트북
- 재현 가능한 10초 segment manifest
- ML 특징 추출 및 SVM 학습 코드
- Log-Mel CNN 학습 코드
- MERT 임베딩 추출 및 분류 코드
- 모델·생성기·장르·압축 조건별 결과표
- 오류 사례 시각화
- 선택 사항: 오디오 업로드 탐지 데모

## 15. 발표 흐름

1. “처음 보는 AI 음악 생성기도 탐지할 수 있는가?”
2. 완전 AI 생성 음악의 조작적 정의와 TTA 선택 이유
3. 확보 데이터와 발견한 품질 문제
4. 원곡 그룹 분할로 데이터 누수 차단
5. 수작업 특징 SVM → Log-Mel CNN → 음악 사전학습 MERT
6. 표준 모델 성능 비교
7. MusicGen·Suno 미지 생성기 성능 하락
8. MP3 압축 전후 강건성
9. 생성기·장르·오류 사례 분석
10. 결론과 탐지 시스템의 한계

## 16. 참고 자료

- [Echoes 데이터셋](https://huggingface.co/datasets/Octavian97/Echoes)
- [Echoes 논문](https://arxiv.org/abs/2603.23667)
- [FMA 공식 저장소](https://github.com/mdeff/fma)
- [MERT 논문](https://arxiv.org/abs/2306.00107)
- [MERT 공식 구현](https://github.com/yizhilll/MERT)

Echoes 논문은 10초 비중첩 구간, 트랙 단위 분할, EER, 미지 생성기 LOPO를
사용한다. 본 연구는 이를 참고하되 TTA만으로 탐지 대상을 제한하고, 경량
CNN과 음악 전용 MERT를 수작업 특징 SVM과 비교하며, 트랙 확률 결합과 MP3
압축 강건성 분석을 추가한다.
